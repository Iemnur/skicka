name: Go

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.17

    - name: Build
      run: |
        go install github.com/google/skicka/gdrive
        go install golang.org/x/crypto/pbkdf2
        go install golang.org/x/net/context
        go install golang.org/x/oauth2
        go install gopkg.in/gcfg.v1
        ls
        go build -v skicka.go

    #- name: Test
    #  run: go test -v ./...
